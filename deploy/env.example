# Copy to /var/www/log-awg/env (root-only readable: chmod 600) and adjust.

AWG_INTERFACE=awg0
AWG_BIN=awg
DATABASE_URL=postgres://log_awg:CHANGE_ME@localhost:5432/log_awg?sslmode=disable

# Optional, defaults shown:
# AWG_EXEC_TIMEOUT=5s
# AWG_POLL_INTERVAL=1m
